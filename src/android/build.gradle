repositories{
    jcenter()
    flatDir{
        dirs 'libs'
    }
}

dependencies {
    compile(name:'ScanditBarcodeCapture', ext:'aar')
	 compile(name:'ScanditCaptureCore', ext:'aar')
	  compile(name:'kotlin-stdlib-jdk7-1.3.61', ext:'jar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}